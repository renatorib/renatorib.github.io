{
  "name": "rena.to",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "next",
    "build": "del .next && next build",
    "export": "del out && next export",
    "deploy": "gh-pages -d out -b master -t"
  },
  "dependencies": {
    "next": "^8.0.0",
    "react": "^16.8.0",
    "react-dom": "^16.8.0"
  },
  "devDependencies": {
    "del-cli": "^1.1.0",
    "gh-pages": "^2.0.1"
  }
}
